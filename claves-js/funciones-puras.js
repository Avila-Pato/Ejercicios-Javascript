const outerFunction = (a, b) => a+ b

console.log(outerFunction(1, 2)); // 3


    // Las funciones puras son funciones que, dadas las mismas entradas, siempre devolverán el mismo resultado y no tienen efectos secundarios observables fuera de la función.